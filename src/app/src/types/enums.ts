export enum Permission {
    READ = 0,
    WRITE = 1,
    DELETE = 2,
    SHARE = 3,
}

export enum TargetType {
    USER = 1,
    ROLE = 2,
}

export enum AccessType {
    PUBLIC = 0,
    PRIVATE = 1,
    RESTRICTED = 2,
}